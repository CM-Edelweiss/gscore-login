# 复制为 .env 后按需修改；未列出的字段走 login_env 默认值

# 监听
HOST=0.0.0.0
PORT=7861
LOG_LEVEL=INFO

#DNALoginSecret和NTELoginSecret一致；留空则不校验签名（启动会有 warning）
SHARED_SECRET=

# 启用的登录列表，ww、dna、nte
login_list=["ww", "dna", "nte"]

# 单个登录会话存活秒数
SESSION_TTL_S=600

# HMAC 签名容忍时差秒数（防重放窗口）
SIG_TTL_S=300